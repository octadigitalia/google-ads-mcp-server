# Story 2.1: Ferramenta Base de Consulta GAQL

**Epic:** 2 (Reporting & Data Collection)
**Status:** Done
**Role:** Developer
**Action:** Implementar a tool `search_ads` e `get_campaign_performance` para execução de consultas GAQL
**Benefit:** Permitir que agentes de IA extraiam dados granulares de performance e estrutura da conta de forma dinâmica

---

## Acceptance Criteria

1. [x] Implementar a tool `search_ads` no servidor MCP.
2. [x] Implementar a tool `list_accessible_customers` para descoberta de contas.
3. [x] Implementar a tool `get_campaign_performance` com parâmetros dinâmicos de data.
4. [x] As ferramentas utilizam `SearchStream` para eficiência.
5. [x] O campo `cost_formatted` (R$) é gerado automaticamente na serialização.
6. [x] Suporte total a contextos MCC e Contas Cliente.

---

## 🤖 CodeRabbit Integration

  Story Type Analysis:
    Primary Type: API
    Secondary Type(s): Integration, Architecture
    Complexity: High

  Specialized Agent Assignment:
    Primary Agents:
      - @dev (implementação da tool)
      - @architect (definição do schema de resposta JSON)

  Quality Gate Tasks:
    - [ ] Pre-Commit (@dev): Validar serialização protobuf -> JSON
    - [ ] Pre-PR (@github-devops): Verificar limites de payload

  CodeRabbit Focus Areas:
    Primary Focus:
      - Eficiência na iteração dos resultados (SearchStream vs Search)
      - Serialização correta de enums e campos repetidos do Google Ads
    Secondary Focus:
      - Documentação da Tool (docstrings claras para o LLM)

---

## Tasks / Subtasks

- [ ] Task 1: Design do Schema de Resposta (AC: 5)
  - [ ] @architect: Definir como converter objetos Protobuf do Google Ads em JSON limpo.
- [ ] Task 2: Implementação da Tool `search_ads` (AC: 1, 2, 3, 4)
  - [ ] Criar a função decorada com `@mcp.tool()`.
  - [ ] Integrar com o `GoogleAdsClient` criado na Epic 1.
- [ ] Task 3: Serialização e Tratamento de Dados (AC: 5, 7)
  - [ ] Implementar conversor Protobuf -> Dict.
  - [ ] Adicionar lógica de paginação ou limites de segurança.
- [ ] Task 4: Tratamento de Erros GAQL (AC: 6)
  - [ ] Capturar `GoogleAdsException` e retornar mensagens úteis.

---

## Dev Notes

### Architecture Context
- **SDK**: `google-ads-python` usa Protobuf. A serialização direta pode ser complexa. [Source: docs/architecture/tech-stack.md]
- **GAQL**: O LLM precisa saber que não pode usar `SELECT *`. As queries devem ser explícitas.

### Testing Standards
- Mockar a resposta do `GoogleAdsService` para testes unitários.
- Localização: `tests/unit/test_reporting.py`.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-27 | 1.0 | Initial story creation for GAQL tool | @aios-master |
