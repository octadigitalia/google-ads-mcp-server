# Story 3.1: Alterar Status de Campanha (Pause/Enable)

**Epic:** 3 (Campaign Lifecycle)
**Status:** Done
**Role:** Developer
**Action:** Implementar a tool `set_campaign_status` para gerenciar a veiculação de campanhas
**Benefit:** Permitir que agentes de IA tomem ações corretivas imediatas baseadas em análise de performance

---

## Acceptance Criteria

1. [x] Implementar a tool `set_campaign_status` no servidor MCP.
2. [x] A tool deve aceitar os parâmetros: `customer_id`, `campaign_id` e `status`.
3. [x] O parâmetro `status` aceita `'ENABLED'`, `'PAUSED'`.
4. [x] Utilizar o `CampaignService.mutate_campaigns` com `FieldMask` automática.
5. [x] Retornar confirmação clara da operação.
6. [x] Sucesso validado em conta real (**TecPlaner**).

---

## 🤖 CodeRabbit Integration

  Story Type Analysis:
    Primary Type: API
    Secondary Type(s): Security
    Complexity: Medium

  Specialized Agent Assignment:
    Primary Agents:
      - @dev (implementação do mutate)
      - @architect (validação do padrão de escrita)

  Quality Gate Tasks:
    - [ ] Pre-Commit (@dev): Validar construção do CampaignOperation
    - [ ] Pre-PR (@github-devops): Validar permissões de escrita no ambiente de homologação (se disponível)

  CodeRabbit Focus Areas:
    Primary Focus:
      - Correta construção da FieldMask para atualização parcial
      - Tratamento de exceções de Mutate (GoogleAdsException)
    Secondary Focus:
      - Logging de auditoria (quem alterou o quê?)

---

## Tasks / Subtasks

- [ ] Task 1: Design do Padrão de Escrita (AC: 4, 5)
  - [ ] @architect: Definir como estruturar operações de escrita para evitar "full updates" desnecessários.
- [ ] Task 2: Implementação da Tool `set_campaign_status` (AC: 1, 2, 3, 4)
  - [ ] Criar a função decorada com `@mcp.tool()`.
  - [ ] Implementar a lógica de `CampaignOperation` e `FieldMask`.
- [ ] Task 3: Validação e Resposta (AC: 5, 6)
  - [ ] Validar strings de status antes da chamada.
  - [ ] Formatar resposta de sucesso/erro para o MCP.

---

## Dev Notes

### Architecture Context
- **SDK**: Utilizar `client.get_service("CampaignService")`.
- **FieldMask**: O Google Ads API exige uma `FieldMask` para atualizações. O SDK Python facilita isso com o `update_mask`.

### Testing Standards
- Teste de integração (manual) usando uma campanha de teste ou pausando/reativando uma campanha real de baixo risco.
- Localização: `tests/integration/test_mutations.py`.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-27 | 1.0 | Initial story creation for Campaign Status management | @aios-master |
